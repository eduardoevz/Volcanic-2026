# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Git commit messages

Do NOT include a Claude session link (e.g. `Claude-Session: https://claude.ai/code/...`)
in commit messages or pull request descriptions pushed to this repo. Reason: privacy —
this repo is public, and that link should not be exposed publicly. If the assistant's
tooling forces this footer regardless of this instruction, flag it to the user rather
than silently including it.
