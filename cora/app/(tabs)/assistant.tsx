import { Screen } from '@/ui/components/Screen';
import { Text } from '@/ui/components/Text';

export default function Assistant() {
  return (
    <Screen>
      <Text variant="title">Cora IA</Text>
      <Text variant="bodyMuted">Asistente con guardrails — llega en la Fase 7.</Text>
    </Screen>
  );
}
